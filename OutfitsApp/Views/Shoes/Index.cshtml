@model IEnumerable<OutfitsApp.Models.Sho>

@{
    ViewBag.Title = "Our Shoes";
}

<h2>Our Shoes</h2>

<p>
    @Html.ActionLink("Upload your own", "Create")
</p>
<div class="row">
    <div class="col-xs-6 col-md-4 col-lg-4">

      </div>
</div>

        @foreach (var item in Model)
        {
           <a href="@Url.Action("Details", new { id = item.ShoeID })"> <img src="@Html.DisplayFor(modelItem => item.ShoePhoto)" style="width:225px;height:195px;" /></a>
        }
